---
galaxy_info:
  role_name: postgresql
  author: Dan Morcov
  description: >-
    Apply a Basewright plan for a PostgreSQL instance: the vendor repository, the packages,
    the cluster, its configuration and the secrets it needs. Reads the plan and nothing
    else.
  license: Apache-2.0
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions: [noble, jammy]
    - name: Debian
      versions: [bookworm]
  galaxy_tags: [postgresql, database, provisioning]
dependencies: []
