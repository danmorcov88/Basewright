"""Sizing evaluation, layout resolution and plan assembly.

Every computed value carries the id of the rule that produced it and the
explanation that rule ships with. The plan is deterministic: identical facts and
identical profiles render byte-identical output.
"""
